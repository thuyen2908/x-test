{
	"extends": "../../tsconfig.json",

	"exclude": ["dist", "test-artifacts", "test-report", "playwright.config.ts"],

	"compilerOptions": {
		"incremental": true,

		"rootDir": "src",
		"baseUrl": "src",

		"paths": {
			"#env": ["env.ts"],
			"#const": ["const.ts"],
			"#typings": ["typings.ts"],

			"~/*": ["*"]
		}
	}
}
