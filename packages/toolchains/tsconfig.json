{
	"extends": "../../tsconfig.json",

	"exclude": ["dist", "vite.config.ts"],

	"compilerOptions": {
		"noEmit": false,
		"incremental": true,

		"baseUrl": "src",
		"rootDir": "src",
		"outDir": "dist"
	}
}
